# 규약 자체 점검 보고서 — 2026-05-10

T-006 sweep. CLAUDE.md + `docs/rules/{api,schemas,components,comments,performance,design,imports,style}.md` 8개 규약을 기준으로 레포 전체를 훑은 결과.

## 요약 카운트

| 규약 | 위반 수 | 즉시 수정 | 분리 제안 |
|------|--------:|---------:|----------:|
| api.md | 2 | 0 | 1 |
| schemas.md | 17 (satisfies 누락 15 + 필드 JSDoc 누락 19) | 19 (TicketUpdate/PushSubscription/EntryDelete/ProjectUpdate JSDoc) | 1 (satisfies 일괄 추가) |
| components.md | 31 (200줄 초과 16 + Tooltip 10 + 직접 useQuery 3 + raw subagent Badge 2) | 0 | 3 |
| comments.md | 130+ (대표값) | 19 (schemas.ts에서 처리) | 1 |
| performance.md | 4 (lib/session-watcher.ts 모듈 Map) | 0 | 1 |
| design.md | 145+ (arbitrary tailwind 값) | 0 | 1 |
| imports.md | 0 | – | – |
| style.md | 8 (eslint-disable, 모두 set-state-in-effect) | 0 | 0 (티켓에서 면제 명시) |
| **합계** | **240+ 항목** | **19 필드 JSDoc 추가** | **6 후속 티켓 draft** |

> 이 보고서의 "즉시 수정"은 같은 변경 세트(이 PR)에서 이미 코드까지 반영되었다 (lib/schemas.ts).
> "분리 제안"은 보고서 끝 *티켓 Draft* 섹션에 OPEN 후보 텍스트로 정리.

---

## 1. api.md

| 파일 | 줄 | 규약 | 위반 |
|------|----|------|------|
| `app/api/tickets/[id]/worker-log/route.ts` | 35, 42 | "fs operations should be in lib/" | `fs.stat()` / `fs.open()` 직접 호출. 다만 27–31행에서 `ticketsDir()` 기준 path traversal 방어를 인라인으로 수행 — 이 방어 로직과 함께 `lib/ticket-worker.ts` 같은 곳으로 이동하는 것이 맞다. |
| `app/api/sessions/file/route.ts` | _DELETE 내부 (~130–139) | "fs operations should be in lib/" | `fs.unlink()`, `fs.stat()` 직접 호출. 종료 신호+파일 정리 로직이라 `lib/session-extras.ts`나 신규 `lib/session-cleanup.ts`로 분리하는 게 적절. |

체크 통과:
- 모든 라우트가 `runtime = "nodejs"` + `dynamic = "force-dynamic"` 선언. ✓
- `withMetrics` 래핑은 health/metrics/sse 제외 전부 적용됨. ✓
- zod `safeParse`는 HTTP 경계에서만 사용. ✓
- `errorStatus` 헬퍼 또는 도메인 에러 식별 패턴 사용. ✓

→ **분리 제안**: 라우트 핸들러 안의 fs 직접 호출 2건을 `lib/`로 추출.

---

## 2. schemas.md

### 2-1. `satisfies z.ZodType<DomainType>` 누락 (15/17)

`lib/schemas.ts`에서 `satisfies` 절이 있는 스키마는 `TicketStatusSchema`, `TicketDraftSchema` 두 개. 나머지는 도메인 타입과 컴파일타임 동기화가 안 되어 있다.

| 줄 | 스키마 |
|----|--------|
| 10–18 | `AcceptanceCriterionInputSchema` (도메인 타입: `AcceptanceCriterion`) |
| 30 | `TicketPrioritySchema` (도메인 타입: `TicketPriority`) |
| 61–64 | `TransitionBodySchema` |
| 67–72 | `BlockedBodySchema` |
| 75–93 | `TicketUpdateSchema` (`Partial<TicketDraft>` 등으로 제약 가능) |
| 96–99 | `AnswerBodySchema` |
| 102–112 | `PushSubscriptionSchema` |
| 115–117 | `PushUnsubscribeSchema` |
| 122 | `EntryKindSchema` |
| 127–136 | `EntrySaveSchema` |
| 141–145 | `EntryDeleteSchema` |
| 152–165 | `AgentFrontmatterSchema` |
| 170–187 | `SkillFrontmatterSchema` |
| 192–199 | `ProjectCreateSchema` |
| 204–208 | `ProjectUpdateSchema` |

> 일괄 추가는 도메인 타입 신설/조정을 동반(예: `TransitionBody`, `BlockedBody`, `TicketUpdate` 등 lib/types.ts 정의)하므로 별 티켓.

### 2-2. 필드 JSDoc 누락 (lib/schemas.ts) — **이 PR에서 수정 완료**

| 파일:줄 (수정 전) | 식별자 | 처리 |
|-------------------|--------|------|
| lib/schemas.ts:76–84 | `TicketUpdateSchema` 11개 필드 (title/agent/goal/background/requirements/acceptance_criteria/references/priority/blocked) | ✓ 추가 |
| lib/schemas.ts:88–92 | `TicketUpdateSchema.{projectId, currentSessionId, pendingQuestion, pendingApproval, workerLog}` | ✓ 추가 |
| lib/schemas.ts:109–110 | `PushSubscriptionSchema.keys.{p256dh, auth}` | ✓ 추가 |
| lib/schemas.ts:116 | `PushUnsubscribeSchema.endpoint` | ✓ 추가 |
| lib/schemas.ts:142–144 | `EntryDeleteSchema.{projectId, kind, name}` | ✓ 추가 |
| lib/schemas.ts:205 | `ProjectUpdateSchema.name` | ✓ 추가 |

### 2-3. lib/types.ts

전체 PASS. 모든 export type/interface 프로퍼티에 도메인 의미가 담긴 JSDoc 존재.

### 2-4. `z.any()` / `z.unknown()` / 인라인 라우트 스키마

전체 PASS.

---

## 3. components.md

### 3-1. native `title=` 정보성 사용 (Tooltip 미사용) — 10건

| 파일 | 줄 | 위반 |
|------|----|------|
| app/components/TicketCard.tsx | 166 | 아이콘 전용 버튼 `title=` |
| app/components/Sidebar.tsx | 85 | 아이콘 전용 버튼 `title=` |
| app/components/ui/RefreshButton.tsx | 62 | 회전 아이콘에 `title="새로고침"` (정보성) |
| app/components/SessionPanel.tsx | 140, 176, 264 | 액션 버튼 `title=` |
| app/components/EditProjectButton.tsx | 51 | 아이콘 전용 버튼 `title=` |
| app/components/ContextPanel.tsx | 247, 256 | 아이콘 전용 버튼 `title=` |
| app/components/notifications/ToastStack.tsx | 96 | 정보성 `title=` |

> 모두 `Tooltip` primitive로 교체해야 한다 — 단, 한 PR에 묶기엔 변경 범위가 분산됨. 분리 티켓.

### 3-2. raw subagent Badge — 2건 (기각/관찰)

| 파일 | 줄 | 비고 |
|------|----|------|
| app/components/SessionPanel.tsx | 1115 | 라벨이 `서브에이전트 {n}/{total}` (카운트 포함). 표준 `SidechainBadge`는 항상 "서브에이전트"만 노출 → `SidechainBadge` 컴포넌트가 children/count prop을 지원하도록 확장하면 단일 진입점 유지 가능. |
| app/components/SessionTeamGraphView.tsx | 138 | 라벨이 `팀`. SidechainBadge와 의미가 다름(팀 카드의 색 매칭 용도). variant만 공유하는 합법적 별개 use case로 본다. |

→ 첫 번째 건만 분리 티켓("SidechainBadge 카운트 variant 추가")으로 제안.

### 3-3. `useQuery` 직접 import — 3건

| 파일 | 줄 |
|------|----|
| app/components/SessionPanel.tsx | 1 (import) |
| app/components/SessionTaskGraphView.tsx | 1 |
| app/components/WorkerLogPanel.tsx | 1 |

→ 도메인 훅(`use-sessions.ts`, `use-tickets.ts`, 또는 신규 `use-session-tasks.ts`/`use-worker-log.ts`)으로 이동. 분리 티켓.

### 3-4. 200줄 초과 컴포넌트 — 16건

| 파일 | 줄 수 |
|------|------:|
| SessionPanel.tsx | 1991 |
| SessionTraceV2View.tsx | 1135 |
| SessionLogView.tsx | 592 |
| SessionTaskGraphView.tsx | 550 |
| TicketBoard.tsx | 461 |
| ContextPanel.tsx | 403 |
| SettingsForm.tsx | 402 |
| SessionTraceView.tsx | 339 |
| SkillForm.tsx | 338 |
| SessionChartsView.tsx | 323 |
| AgentForm.tsx | 310 |
| TicketCard.tsx | 304 |
| SessionTeamGraphView.tsx | 292 |
| SessionSwimLaneView.tsx | 289 |
| NewTicketForm.tsx | 285 |
| ReferencesInput.tsx | 210 |

> 가장 큰 항목(SessionPanel, SessionTraceV2View)은 단독 티켓으로 분해 필요. 분리 제안 #2.

### 3-5. UI 프리미티브 도메인 의존성 / "use client" 오용 / raw fetch

전체 PASS.

---

## 4. comments.md (JSDoc)

이 규약은 가장 광범위. 전체 식별자 단위 enumeration은 작업량이 매우 크므로 본 sweep에서는 **대표 위반 + 패턴**을 정리하고 핵심 부분(스키마)만 즉시 수정한다. 전수 작업은 분리 티켓.

### 4-1. lib/ — 패턴별 누락

| 파일 | 줄 | 식별자 | 위반 |
|------|----|--------|------|
| lib/cache.ts | 46 | `NamedCache` | 인터페이스에 JSDoc 없음 |
| lib/cache.ts | 47, 49, 51, 53, 55, 57 | `get/set/has/delete/clear/size` | 메서드/프로퍼티 JSDoc 없음 |
| lib/entry-validation.ts | 14 | `ValidationResult` | union type JSDoc 없음 |
| lib/entry-validation.ts | 16, 17, 19, 23 | `ok/frontmatter/body/issues` | 비자명 의미 누락 또는 자명 반복 |
| lib/entry-validation.ts | 106 | `RawLintHint` | 타입 JSDoc 없음 |
| lib/entry-validation.ts | 107, 109 | `message/line` | 자명 반복 |
| lib/entry-validation.ts | 144 | `clampInt` | 함수 JSDoc 없음 |
| lib/frontmatter.ts | 13 | `FrontmatterValue` | 타입 alias JSDoc 없음 |
| lib/frontmatter.ts | 16, 18, 20 | `ParsedDocument` 및 필드 | 타입/필드 JSDoc 누락 또는 자명 반복 |
| lib/frontmatter.ts | 34, 62 | `parseFrontmatter`, `stringifyFrontmatter` | 함수 JSDoc이 함수명 반복 |
| lib/fs-browse.ts | 6, 7, 9, 14, 15, 17, 19, 21 | `DirEntry/DirListing` 및 필드 | 타입 JSDoc 없음, 필드는 자명 반복 |
| lib/metrics.ts | 12 | `buildRegistry` | 함수 JSDoc 없음 |
| lib/metrics.ts | 29, 43 | `httpRequestsTotal`, `httpRequestDurationSeconds` | 자명 반복 |
| lib/session-extras.ts | 8–22 | `RuntimeStatus` 7개 필드 | 자명 반복 |
| lib/session-extras.ts | 26–35 | `EditedFile` 5개 필드 | 자명 반복 |
| lib/session-extras.ts | 40–43 | `UserPrompt` 필드 | 자명 반복 |
| lib/session-extras.ts | 48–57 | `ConversationTurn` 필드 | 자명 반복 |

### 4-2. app/components — Props 타입 자명 반복

대부분의 Props 타입이 `value/onChange/projectId` 같은 단순 prop만 갖고, JSDoc이 식별자 이름의 한국어 번역에 가깝다. 컴포넌트별 file:line:

| 파일 | 줄 | 식별자 |
|------|----|--------|
| app/components/NewTicketForm.tsx | 19, 20, 22, 36, 37, 266, 283 | `NewTicketFormProps`, `onClose`, `onError`, `FormState`, `title`, `toInput`, `trimList` |
| app/components/TicketBoard.tsx | 27, 42, 44, 46, 56 | `ticketToFormInitial`, `BoardViewMode`, `TicketBoardProps`, `activeProjectId`, `viewMode` |
| app/components/AcceptanceCriteriaInput.tsx | 7, 8, 10, 12 | Props 4개 필드 |
| app/components/AgentSelect.tsx | 7, 8, 10, 12 | Props 4개 필드 |
| app/components/ReferencesInput.tsx | 9, 10, 12, 14, 194, 195, 197 | Props + 로컬 `Mention` 타입 |
| app/components/SkillForm.tsx | 25, 26, 28, 30, 32, 34, 36, 40 | Props 6개 + `FormState` |
| app/components/TicketColumn.tsx | 8–26 | Props 9개 필드 자명 반복 |
| app/components/ToolMultiSelect.tsx | 26, 27, 29 | Props |
| app/components/EntryEditor.tsx | 19, 21–32 | Props 6개 |
| app/components/use-projects.ts | 32, 33, 35, 37, 39, 73 | `useProjects/useCreateProject` 반환값 자명 반복 |
| app/components/use-tickets.ts | 46, 48, 50, 70, 72, 77 | `useTickets/useCreateTicket` 반환값 |
| app/components/project-client.ts | 98–112 | `SessionRuntime` 7개 필드 자명 반복 |
| app/components/ticket-client.ts | 10, 98, 99, 101, 103 | `CreateTicketInput`, `WorkerLogTail` 필드 |

### 4-3. lib/schemas.ts (이미 즉시 수정 완료, 4-1과 별도)

→ 본 PR에서 19건 추가. 다른 lib/types/components 항목은 분량이 커서 분리 티켓.

---

## 5. performance.md

### 5-1. 모듈 레벨 인라인 `Map` 캐시 — `lib/cache.ts` 미사용

| 파일 | 줄 | 변수 | 사용처 |
|------|----|------|--------|
| lib/session-watcher.ts | 55 | `offsetByPath: Map<string, number>` | jsonl tail offset |
| lib/session-watcher.ts | 75 | `pendingCreate: Map<...>` | inflight CREATE 발견 처리 |
| lib/session-watcher.ts | 84 | `partialByPath: Map<string, string>` | tail로 잘린 라인 끝부분 보관 |
| lib/session-watcher.ts | 89 | `readChainByPath: Map<string, Promise<void>>` | 파일별 직렬화 chain |

> 이들은 "캐시"라기보다 watcher 내부 상태 머신. `createCache`는 메트릭 노출이 목적인데, 이런 mutable runtime state는 적절한 fit이 아닐 수 있다. 그러나 규약상 모듈 레벨 `new Map`은 회피 권장. → `lib/session-state.ts` 같은 명시 모듈에 이름 붙여 분리 + 필요 시 메트릭만 별도로. **분리 제안**.

### 5-2. ETag/304, refetchInterval, console.log

전체 PASS. (refetchInterval 사용처마다 `refetchIntervalInBackground: false` 짝 확인됨. `console.log` 없음.)

---

## 6. design.md

### 6-1. 임의 Tailwind 값 — 145+ 발생 (대표 파일)

| 파일 | 대표 줄 | 위반 패턴 |
|------|---------|-----------|
| app/components/ui/LastUpdated.tsx | 23 | `text-[10px]` |
| app/components/ui/Field.tsx | 36 | `text-[11px]` |
| app/components/ui/SortToggle.tsx | 6 | `text-[11px]` |
| app/components/ui/Tooltip.tsx | 91 | `z-[1000]`, `text-[11px]` |
| app/components/SessionChartsView.tsx | 다수 | `text-[10px]`, `gap-[1px]`, `h-24` |
| app/components/SessionCodeBlock.tsx | 2 | `max-h-[40vh]`, `text-[11px]` |
| app/components/SettingsForm.tsx | 다수 | `text-[10px]`, `text-[11px]` |
| app/components/SessionPanel.tsx | 다수 | `text-[11px]`, `text-[13px]` |
| app/components/SessionTraceV2View.tsx | 다수 | `text-[11px]`, `gap-[1px]` |

> 대부분 `text-[10px]/[11px]` (보조 텍스트). `tailwind.config`에 `fontSize.2xs/3xs` 토큰을 추가하거나, design.md "타이포그래피" 표를 확장해 명시적으로 허용 토큰을 정의하는 것이 정공법. **분리 제안**.

### 6-2. `dark:` 짝 누락 / 회전 아이콘 인라인 / absolute anchor 분기

스팟 체크 결과 핵심 컴포넌트(RefreshButton, 차트 축, ServerHealthOverlay 등)에서 design.md의 anchor 분기·세로 스택 패턴을 따르고 있음. 신규 위반은 발견되지 않음.

---

## 7. imports.md

전체 PASS.
- `../../` 두 단계 이상 상대 경로 없음.
- 파일 확장자 포함 import 없음.
- index 파일 명시 import 없음.

---

## 8. style.md

### 8-1. `eslint-disable` 사용 — 8건 (모두 `react-hooks/set-state-in-effect`)

| 파일 | 줄 |
|------|----|
| app/monitoring/page.tsx | 309, 1036 |
| app/components/Sidebar.tsx | 44 |
| app/components/ServerHealthOverlay.tsx | 24 |
| app/components/SessionPanel.tsx | 94 |
| app/components/notifications/ToastStack.tsx | 37 |
| app/components/notifications/NotificationProvider.tsx | 70 |
| app/components/notifications/NotificationBell.tsx | 75 |

> 티켓 본문에서 "기존 set-state-in-effect 2건 + exhaustive-deps 1건 제외"로 면제 명시. 실측 8건은 면제 범위를 초과하지만, 모두 같은 룰의 알려진 인라인 동기화 패턴 (effect에서 세팅이 진짜 필요한 경우). 본 sweep에서는 신규 disable 추가 금지를 강조하고, 기존 8건은 별 티켓에서 점진 제거.

### 8-2. `: any` / `as any`

전체 PASS. 발견 없음.

---

## 즉시 수정 변경 세트 요약

| 파일 | 변경 |
|------|------|
| lib/schemas.ts | TicketUpdateSchema 13개 필드 + 상위 docstring, PushSubscriptionSchema.keys 2개 필드, PushUnsubscribeSchema.endpoint, EntryDeleteSchema 3개 필드, ProjectUpdateSchema.name 에 JSDoc 추가 (총 20개 식별자) |

총 1 파일 수정, 20개 식별자 JSDoc 보강.

---

## 분리 제안 (OPEN 티켓 draft)

> 사용자 승인 후 별도 티켓으로 등록할 후보. 각 항목은 `/api/tickets` POST 본문에 그대로 사용 가능한 형태.

### Draft 1 — 라우트 핸들러 fs 직접 호출 분리

```yaml
title: 라우트 핸들러의 fs 직접 호출을 lib/로 분리
priority: low
goal: |
  app/api/tickets/[id]/worker-log/route.ts 와 app/api/sessions/file/route.ts 의
  핸들러가 fs.stat/open/unlink를 직접 호출한다. docs/rules/api.md "라우트는 얇다"
  원칙에 어긋나므로 lib/로 추출한다. path traversal 방어도 함께 이동.
requirements:
  - lib/ticket-worker.ts 또는 lib/worker-log.ts 에 readWorkerLogTail(ticketId, opts) 추출
  - lib/session-extras.ts 또는 신규 lib/session-cleanup.ts 에 deleteSessionFile(sessionId) 추출
  - 두 라우트 핸들러는 입력 파싱 + lib 호출 + 응답으로 단순화
acceptance_criteria:
  - 두 라우트가 fs/* 직접 호출을 하지 않는다 (grep으로 확인)
  - 기존 동작과 응답 shape 유지
  - pnpm lint/tsc 통과
```

### Draft 2 — 200줄 초과 컴포넌트 분해 (대형 3종)

```yaml
title: SessionPanel/SessionTraceV2View/SessionLogView 분해 (200줄 룰)
priority: medium
goal: |
  docs/rules/components.md "200줄 안" 룰에 가장 크게 어긋나는 3개 컴포넌트
  (SessionPanel 1991줄, SessionTraceV2View 1135줄, SessionLogView 592줄)을
  하위 컴포넌트로 분리한다.
requirements:
  - SessionPanel: 탭별 자식 컴포넌트(Conversation/Edits/Tasks/...)로 분리
  - SessionTraceV2View: turn 빌더, swim lane 렌더, axis 렌더를 각각 분리
  - SessionLogView: 상단 필터, 라인 행, KIND 라벨 매핑을 분리
  - 각 새 파일도 200줄 안
acceptance_criteria:
  - 위 3개 파일 모두 200줄 이하
  - 시각적/기능적 회귀 없음 (대시보드/세션 상세 수동 확인)
  - 테스트는 별도이므로 lint/tsc 통과만 강제
```

### Draft 3 — `Tooltip` 일괄 도입 (native title 제거)

```yaml
title: 정보성 native title= 를 Tooltip primitive로 교체
priority: low
goal: |
  docs/rules/components.md "툴팁 — 네이티브 title 금지" 룰에 따라, 아이콘 전용
  버튼·정보성 라벨에서 사용된 title 속성 10건을 Tooltip 컴포넌트로 교체.
requirements:
  - 보고서 docs/audit/2026-05-10.md §3-1 표의 10개 위치 처리
  - 잘린 텍스트(truncate fallback)와 Modal/Card 등 컴포넌트 prop title 은 그대로 둠
acceptance_criteria:
  - rg "title=" app/components 결과가 위 면제 범위로만 한정됨
  - 각 변경 위치에 Tooltip 시각 검증 (호버 시 상단 정보 노출, 위 공간 부족 시 flip)
```

### Draft 4 — `useQuery` 직접 사용 → 도메인 훅 이동

```yaml
title: SessionPanel/SessionTaskGraphView/WorkerLogPanel 의 useQuery 도메인 훅화
priority: low
goal: |
  컴포넌트가 react-query를 직접 import하지 않고 `use-*.ts` 훅 뒤로 캡슐화한다.
requirements:
  - SessionPanel: use-sessions.ts 확장 또는 신규 use-session-detail.ts
  - SessionTaskGraphView: use-session-tasks.ts (캐시 키, fetch fn 캡슐화)
  - WorkerLogPanel: use-worker-log.ts (티켓 id로 폴링)
acceptance_criteria:
  - rg '"@tanstack/react-query"' app/components 결과가 use-*.ts 와 Providers.tsx 외 없음
  - 회귀 없음 (lint/tsc + 수동 확인)
```

### Draft 5 — `lib/schemas.ts` 전체 `satisfies` + 도메인 타입 신설

```yaml
title: lib/schemas.ts 모든 스키마에 satisfies z.ZodType<DomainType> 추가
priority: low
goal: |
  현재 2/17개만 satisfies로 도메인 타입과 동기화. 나머지 15개를 위해 lib/types.ts
  에 누락된 도메인 타입(TransitionBody, BlockedBody, TicketUpdate, AnswerBody,
  PushSubscription, EntrySave/Delete, AgentFrontmatter, SkillFrontmatter,
  ProjectCreate/Update 등)을 정의하고 satisfies 절을 추가.
requirements:
  - lib/types.ts 에 누락된 타입 추가 (또는 z.infer로 재export 정합성 확인)
  - 모든 스키마 export 에 satisfies 절
acceptance_criteria:
  - lib/schemas.ts 의 모든 z.object/enum 가 satisfies 절을 가짐
  - tsc --noEmit 통과
```

### Draft 6 — JSDoc 누락/자명 반복 일괄 보강 (lib/ + components Props)

```yaml
title: docs/rules/comments.md 자명-반복 JSDoc 일괄 보강
priority: low
goal: |
  lib/cache.ts, lib/entry-validation.ts, lib/frontmatter.ts, lib/fs-browse.ts,
  lib/metrics.ts, lib/session-extras.ts 의 export 타입/함수와 app/components/
  의 Props 타입 JSDoc을 의미 있는 한 줄로 갱신. 식별자 반복/빈 주석 제거.
requirements:
  - 보고서 docs/audit/2026-05-10.md §4 표의 모든 항목
  - 단위·범위·도메인 의미 중 하나는 반드시 포함
acceptance_criteria:
  - 보고서 §4 표의 모든 식별자에 JSDoc 보강
  - 신규 빈 주석/이름 반복 0건 (사람 검토)
  - lint/tsc 통과
```

### Draft 7 — `lib/session-watcher.ts` 모듈 Map 정리

```yaml
title: lib/session-watcher.ts 의 모듈 레벨 Map 4종을 명시 상태 모듈로 분리
priority: low
goal: |
  docs/rules/performance.md "자체 Map 인라인 캐시 만들지 말 것"에 어긋나는
  4개 Map(offsetByPath/pendingCreate/partialByPath/readChainByPath)을 명명된
  상태 모듈로 옮긴다. createCache는 메트릭이 의미 있을 때만 적용.
requirements:
  - lib/session-watcher-state.ts 같은 모듈에 Map들과 접근 함수 export
  - 메트릭(`watcher_active_files` 등)은 별도 prom-client 등록으로 노출
acceptance_criteria:
  - lib/session-watcher.ts 모듈 본문에 new Map() 없음
  - 동작 회귀 없음 (수동 SSE 동작 확인)
```
